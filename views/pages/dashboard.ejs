<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #f8f9fa;
      padding: 3rem 1rem;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }
    h1 {
      font-weight: 700;
      margin-bottom: 2rem;
      color: #1a1a1a;
    }
    .card {
      border-radius: 0.7rem;
      box-shadow: 0 0.15rem 0.5rem rgba(0,0,0,0.1);
    }
    .btn-create-post {
      font-size: 1.25rem;
      padding: 0.625rem 1.5rem;
    }
    .post-actions a {
      margin-left: 1.5rem;
      font-weight: 500;
      cursor: pointer;
      text-decoration: none;
    }
    .post-actions a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container" style="max-width: 600px;">
    <h1 class="text-center">Admin Dashboard</h1>

    <div class="card mb-4 p-4">
      <a href="/admin/create" class="btn btn-primary btn-create-post">Create Post</a>
    </div>

    <div class="card p-3">
      <h4 class="mb-3">Posts</h4>
      <% findallad.forEach((item=>{ %>
     <div class="d-flex justify-content-between align-items-center border-top pt-3">
        <div class="fs-6 fw-semibold"><%= item.title %></div>
        <div class="post-actions text-primary">
          <a href="/admin/edit/<%= item.slug %>" tabindex="0" aria-label="Edit My First Post">Edit</a>
          <a href="/admin/delete/<%= item.slug %>" onclick="return confirm('Are you sure you want to delete this blog?');" tabindex="0" aria-label="Delete My First Post">Delete</a>
          <a href="/blog/<%= item.slug %>" tabindex="0" aria-label="View My First Post">View</a>
        </div>
      </div>
     <% })) %>

    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

