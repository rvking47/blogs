<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= data.title %></title>
  <meta name="description" content="<%= data.title %>">
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 40px;
      background: #f9f9f9;
      color: #333;
      line-height: 1.6;
    }
    .container {
      max-width: 800px;
      margin: auto;
      background: #fff;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    h1 {
      font-size: 32px;
      margin-bottom: 20px;
      color: #222;
    }
    .content {
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <div style="width: 50%;" class="container">
    <h1>Tittle : <%= data.title %></h1>
    <div class="content">
     Content : <%- data.content %>
      <!-- Using <%- %> to render raw HTML from Quill -->
    </div>
  </div>
</body>
</html>
